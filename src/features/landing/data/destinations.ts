/**
 * @deprecated Must-Visit now loads real destinations from the database via
 * `getMustVisitDestinations`. Kept only so older imports fail loudly if any remain.
 */
export const DESTINATION_MEDIA = [] as const;
